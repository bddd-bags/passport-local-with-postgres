# passport-local-with-postgres

### how to do it?

```run in terminal : npm run dev || npm run start```

```create & migrate database : sequelize db:create && sequelize db:migrate```

```jangan lupa ubah username dan password di config.js```
